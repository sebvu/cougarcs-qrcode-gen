# CougarCS QRcode Generator 🐺

- Auto generate colored QRcodes for CougarCS events.

## Installation 🖥️

### Windows

- install WSL (comes with ubuntu by default)
- run

```
sudo apt install imagemagick qrencode
```

### Mac

- install brew package manager
- run

```
brew install qrencode imagemagick
```

### Linux

- install `qrencode` and `imagemagick` with ur fav package manager on ur distro

## Usage 🛠️

this script requires the package `qrencode` and `imagemagick`

clone it anywhere

```
git clone https://github.com/sebvu/cougarcs-qrcode-gen.git

or

git clone git@github.com:sebvu/cougarcs-qrcode-gen.git
```

run `./cougarcs-qrcode.sh` for usage flags

## Note ❗

- this is a really straight forward script, don't delete any of the folders or files or you'll mess it up like blegh

- the collab option is not done yet, everything else *should* work

- contributions r welcome

## stuff to do

- finish collab option
- refactor

dm me on discord `sebvu` for any basic questions
